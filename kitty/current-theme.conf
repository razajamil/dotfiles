# ===== lighter — LOCAL repo (development) ============================
# Colors come from the local build at ~/dev/lighter. After editing the palette
# there, run `make build` (regenerates this file), then reload kitty — or just
# `make reload` from the repo to rebuild + live-apply in one step.
# Use the published theme instead: replace the path below with  lighter.conf
# Revert to the old theme: delete the include below (originals commented above).
include /Users/raza.jamil/dev/lighter/extras/kitty/lighter.conf
